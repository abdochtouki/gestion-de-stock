//pour creer un base de données
CREATE DATABASE stockage;
// pour creer le tableau stocks
CREATE TABLE stocks (
    item_id INT PRIMARY KEY,
    name VARCHAR(100),
    price DECIMAL(10, 2),
    quantity INT,
    category VARCHAR(50),
    date TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
